package com.example.steplistdemo.model

/**
 * Shared response code constants used across business steps and controllers.
 *
 * [object] defines a singleton holder for constants (similar to a Java utility class).
 */
object ResponseCode {
    const val SUCCESS_CODE = "200"
    const val SUCCESS_CODE_DESC = "Success"
    const val FAIL_CODE = "400"
}
